---
phase: 02-connection-and-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/login/textcodec.go
  - internal/login/textcodec_test.go
  - internal/login/negotiation.go
  - internal/login/negotiation_test.go
  - internal/login/params.go
  - internal/login/errors.go
autonomous: true
requirements: [LOGIN-02, LOGIN-06, TEST-04]

must_haves:
  truths:
    - "Text key=value pairs round-trip through encode/decode with byte-perfect fidelity"
    - "Negotiation engine resolves all five negotiation types (ListSelect, BooleanAnd, BooleanOr, NumericalMin, Declarative) correctly"
    - "NegotiatedParams struct has typed fields for all 14 mandatory RFC 7143 Section 13 keys"
    - "LoginError exposes StatusClass and StatusDetail and works with errors.As()"
    - "Parameterized tests cover the full negotiation matrix per TEST-04"
  artifacts:
    - path: "internal/login/textcodec.go"
      provides: "Text key=value null-byte codec"
      exports: ["EncodeTextKV", "DecodeTextKV", "KeyValue"]
    - path: "internal/login/negotiation.go"
      provides: "Declarative key registry and negotiation engine"
      exports: ["NegotiationType", "KeyDef", "Negotiate", "keyRegistry"]
    - path: "internal/login/params.go"
      provides: "NegotiatedParams struct and defaults"
      exports: ["NegotiatedParams", "Defaults"]
    - path: "internal/login/errors.go"
      provides: "LoginError type and status constants"
      exports: ["LoginError", "StatusSuccess", "StatusAuthFailure"]
  key_links:
    - from: "internal/login/negotiation.go"
      to: "internal/login/params.go"
      via: "Negotiate populates NegotiatedParams fields"
      pattern: "NegotiatedParams"
    - from: "internal/login/negotiation.go"
      to: "internal/login/textcodec.go"
      via: "Negotiate uses DecodeTextKV to parse response keys"
      pattern: "DecodeTextKV"
---

<objective>
Build the text key-value codec, declarative negotiation engine, NegotiatedParams struct, and LoginError type for the iSCSI login phase.

Purpose: These are the pure-logic foundations that the login state machine (Plan 03) and CHAP (Plan 02) build upon. The negotiation engine processes all RFC 7143 Section 13 mandatory keys using a declarative registry pattern (D-04), and the NegotiatedParams struct (D-05) provides compile-time safe access to negotiated values.

Output: `internal/login/` package with text codec, negotiation engine, typed params, and error types -- all with comprehensive tests.
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connection-and-login/02-CONTEXT.md
@.planning/phases/02-connection-and-login/02-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs to know about -->

From internal/pdu/initiator.go:
```go
type LoginReq struct {
    Header
    Transit    bool
    Continue   bool
    CSG        uint8
    NSG        uint8
    VersionMax uint8
    VersionMin uint8
    ISID       [6]byte
    TSIH       uint16
    CID        uint16
    CmdSN      uint32
    ExpStatSN  uint32
    Data       []byte // key-value pairs
}
```

From internal/pdu/target.go:
```go
type LoginResp struct {
    Header
    Transit       bool
    Continue      bool
    CSG           uint8
    NSG           uint8
    VersionMax    uint8
    VersionMin    uint8
    ISID          [6]byte
    TSIH          uint16
    StatusClass   uint8
    StatusDetail  uint8
    Data          []byte
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Text codec, NegotiatedParams, and LoginError</name>
  <files>internal/login/textcodec.go, internal/login/textcodec_test.go, internal/login/params.go, internal/login/errors.go</files>
  <read_first>
    - internal/pdu/initiator.go (LoginReq.Data field -- the data segment that carries text key-value pairs)
    - internal/pdu/target.go (LoginResp.Data field and StatusClass/StatusDetail fields)
    - .planning/phases/02-connection-and-login/02-RESEARCH.md (Pattern 3: Text Key-Value Codec, NegotiatedParams struct, LoginError examples)
  </read_first>
  <behavior>
    - EncodeTextKV([]KeyValue{{"key1","val1"},{"key2","val2"}}) produces bytes "key1=val1\x00key2=val2\x00"
    - DecodeTextKV([]byte("key1=val1\x00key2=val2\x00")) returns []KeyValue{{"key1","val1"},{"key2","val2"}}
    - DecodeTextKV handles trailing null, empty input, key with empty value ("key=\x00"), comma-separated list values
    - EncodeTextKV preserves insertion order (not random like map iteration)
    - Defaults() returns NegotiatedParams with all RFC 7143 defaults (HeaderDigest=false, MaxRecvDataSegmentLength=8192, MaxBurstLength=262144, etc.)
    - LoginError.Error() returns formatted string with class, detail, and message
    - errors.As(&LoginError{}) works on wrapped LoginError
  </behavior>
  <action>
    Create `internal/login/` package directory.

    **textcodec.go** (per D-04, research Pattern 3):
    - Define `type KeyValue struct { Key, Value string }`
    - `func EncodeTextKV(pairs []KeyValue) []byte` -- iterates pairs in order, writes "key=value\0" for each. Uses bytes.Buffer. Deterministic order (slice, not map).
    - `func DecodeTextKV(data []byte) []KeyValue` -- splits on 0x00, then strings.Cut on "=". Skips empty segments (from trailing null). Returns ordered slice of KeyValue.
    - Important: handle edge case where data is nil or empty (return nil slice). Handle "key=" (empty value) correctly -- Value should be "".

    **params.go** (per D-05):
    - Define `type NegotiatedParams struct` with these exact fields:
      ```
      HeaderDigest             bool
      DataDigest               bool
      MaxConnections           uint32
      InitialR2T               bool
      ImmediateData            bool
      MaxRecvDataSegmentLength uint32
      MaxBurstLength           uint32
      FirstBurstLength         uint32
      DefaultTime2Wait         uint32
      DefaultTime2Retain       uint32
      MaxOutstandingR2T        uint32
      DataPDUInOrder           bool
      DataSequenceInOrder      bool
      ErrorRecoveryLevel       uint32
      TargetName               string
      TSIH                     uint16
      ```
    - `func Defaults() NegotiatedParams` returns: HeaderDigest=false, DataDigest=false, MaxConnections=1, InitialR2T=true, ImmediateData=true, MaxRecvDataSegmentLength=8192, MaxBurstLength=262144, FirstBurstLength=65536, DefaultTime2Wait=2, DefaultTime2Retain=20, MaxOutstandingR2T=1, DataPDUInOrder=true, DataSequenceInOrder=true, ErrorRecoveryLevel=0

    **errors.go** (per D-08, D-09):
    - Define `type LoginError struct { StatusClass uint8; StatusDetail uint8; Message string }`
    - `func (e *LoginError) Error() string` returns `fmt.Sprintf("iscsi login: class=%d detail=%d: %s", e.StatusClass, e.StatusDetail, e.Message)`
    - Status constants as uint16 (class<<8 | detail): StatusSuccess=0x0000, StatusRedirectTemp=0x0101, StatusRedirectPerm=0x0102, StatusInitiatorError=0x0200, StatusAuthFailure=0x0201, StatusForbidden=0x0202, StatusTargetNotFound=0x0203, StatusTargetRemoved=0x0204, StatusTargetError=0x0300, StatusServiceUnavailable=0x0301, StatusOutOfResources=0x0302
    - Helper `func statusMessage(class, detail uint8) string` mapping status codes to human-readable messages
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/login/ -run "TestTextCodec|TestDefaults|TestLoginError" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/login/textcodec.go contains `func EncodeTextKV(pairs []KeyValue) []byte`
    - internal/login/textcodec.go contains `func DecodeTextKV(data []byte) []KeyValue`
    - internal/login/textcodec.go contains `type KeyValue struct`
    - internal/login/params.go contains `type NegotiatedParams struct`
    - internal/login/params.go contains `func Defaults() NegotiatedParams`
    - internal/login/params.go contains `MaxRecvDataSegmentLength uint32`
    - internal/login/errors.go contains `type LoginError struct`
    - internal/login/errors.go contains `StatusAuthFailure`
    - internal/login/textcodec_test.go contains at least 5 test cases (round-trip, empty, trailing null, empty value, comma-separated)
    - `go test ./internal/login/ -run "TestTextCodec|TestDefaults|TestLoginError" -count=1` exits 0
    - `go vet ./internal/login/` exits 0
  </acceptance_criteria>
  <done>Text codec round-trips key-value pairs, NegotiatedParams has all 14 mandatory fields with correct defaults, LoginError works with errors.As()</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Declarative negotiation engine with parameterized tests</name>
  <files>internal/login/negotiation.go, internal/login/negotiation_test.go</files>
  <read_first>
    - internal/login/textcodec.go (KeyValue type and codec functions -- just created in Task 1)
    - internal/login/params.go (NegotiatedParams struct -- just created in Task 1)
    - .planning/phases/02-connection-and-login/02-RESEARCH.md (Pattern 2: Declarative Key Registry, RFC 7143 Section 13 Complete Mandatory Key Registry table, Pitfall 4: MaxRecvDataSegmentLength is Declarative)
  </read_first>
  <behavior>
    - BooleanAnd("Yes","Yes")="Yes", BooleanAnd("Yes","No")="No", BooleanAnd("No","Yes")="No", BooleanAnd("No","No")="No"
    - BooleanOr("Yes","Yes")="Yes", BooleanOr("Yes","No")="Yes", BooleanOr("No","Yes")="Yes", BooleanOr("No","No")="No"
    - NumericalMin(262144, 131072)=131072, NumericalMin(512, 16777215)=512
    - ListSelect(["CRC32C","None"], "CRC32C")="CRC32C", ListSelect(["CRC32C","None"], "None")="None"
    - Declarative: each side keeps its own value (MaxRecvDataSegmentLength)
    - NumericalMax(1, 65535)=65535
    - Full negotiation of all 14 keys from target response populates NegotiatedParams correctly
    - FirstBurstLength must not exceed MaxBurstLength after negotiation
    - Out-of-range numeric values are clamped to valid range
  </behavior>
  <action>
    **negotiation.go** (per D-04):
    - Define `type NegotiationType int` with constants: ListSelect, BooleanAnd, BooleanOr, NumericalMin, NumericalMax, Declarative
    - Define `type KeyDef struct { Name string; Type NegotiationType; Default string; Min uint32; Max uint32; RFCRef string }`
    - Define package-level `var keyRegistry = []KeyDef{...}` with all 14 mandatory keys from RFC 7143 Section 13:
      - HeaderDigest: ListSelect, default "None", RFCRef "RFC 7143 Section 13.1"
      - DataDigest: ListSelect, default "None", RFCRef "RFC 7143 Section 13.2"
      - MaxConnections: NumericalMin, default "1", min=1, max=65535, RFCRef "RFC 7143 Section 13.3"
      - InitialR2T: BooleanOr, default "Yes", RFCRef "RFC 7143 Section 13.10"
      - ImmediateData: BooleanAnd, default "Yes", RFCRef "RFC 7143 Section 13.11"
      - MaxRecvDataSegmentLength: Declarative, default "8192", min=512, max=16777215, RFCRef "RFC 7143 Section 13.12"
      - MaxBurstLength: NumericalMin, default "262144", min=512, max=16777215, RFCRef "RFC 7143 Section 13.13"
      - FirstBurstLength: NumericalMin, default "65536", min=512, max=16777215, RFCRef "RFC 7143 Section 13.14"
      - DefaultTime2Wait: NumericalMin, default "2", min=0, max=3600, RFCRef "RFC 7143 Section 13.15"
      - DefaultTime2Retain: NumericalMin, default "20", min=0, max=3600, RFCRef "RFC 7143 Section 13.16"
      - MaxOutstandingR2T: NumericalMin, default "1", min=1, max=65535, RFCRef "RFC 7143 Section 13.17"
      - DataPDUInOrder: BooleanOr, default "Yes", RFCRef "RFC 7143 Section 13.18"
      - DataSequenceInOrder: BooleanOr, default "Yes", RFCRef "RFC 7143 Section 13.19"
      - ErrorRecoveryLevel: NumericalMin, default "0", min=0, max=2, RFCRef "RFC 7143 Section 13.20"
    - `func resolveKey(def KeyDef, initiatorValue, targetValue string) (string, error)` -- applies the negotiation algorithm based on def.Type
    - `func buildInitiatorKeys(cfg *loginConfig) []KeyValue` -- generates the initiator's key proposals for operational negotiation based on config options. Uses keyRegistry defaults unless overridden by functional options.
    - `func applyNegotiatedKeys(params *NegotiatedParams, keys []KeyValue)` -- takes resolved key-value pairs and sets the corresponding fields on NegotiatedParams. Parses "Yes"/"No" to bool, numeric strings to uint32.
    - Post-negotiation validation: if FirstBurstLength > MaxBurstLength, set FirstBurstLength = MaxBurstLength

    **negotiation_test.go** (TEST-04):
    - Table-driven parameterized tests for each negotiation type:
      - TestNegotiationBooleanAnd: all 4 combinations (Yes/Yes, Yes/No, No/Yes, No/No)
      - TestNegotiationBooleanOr: all 4 combinations
      - TestNegotiationNumericalMin: boundary cases (both equal, initiator smaller, target smaller, min/max range edges)
      - TestNegotiationNumericalMax: same pattern
      - TestNegotiationListSelect: CRC32C/None combinations, single-item lists, no overlap (should error)
      - TestNegotiationDeclarative: each side independently declares value
    - TestNegotiateFullParams: end-to-end test that builds initiator keys, creates a mock target response, resolves all keys, and verifies resulting NegotiatedParams matches expected values
    - TestFirstBurstLengthClamping: verify FirstBurstLength is clamped to MaxBurstLength
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go test ./internal/login/ -run "TestNegotiation|TestNegotiate|TestFirstBurst" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/login/negotiation.go contains `type NegotiationType int`
    - internal/login/negotiation.go contains `type KeyDef struct`
    - internal/login/negotiation.go contains `var keyRegistry`
    - internal/login/negotiation.go contains `func resolveKey(`
    - internal/login/negotiation.go contains `func applyNegotiatedKeys(`
    - internal/login/negotiation.go contains all 14 key names: HeaderDigest, DataDigest, MaxConnections, InitialR2T, ImmediateData, MaxRecvDataSegmentLength, MaxBurstLength, FirstBurstLength, DefaultTime2Wait, DefaultTime2Retain, MaxOutstandingR2T, DataPDUInOrder, DataSequenceInOrder, ErrorRecoveryLevel
    - internal/login/negotiation_test.go contains TestNegotiationBooleanAnd with at least 4 subtests
    - internal/login/negotiation_test.go contains TestNegotiationBooleanOr with at least 4 subtests
    - internal/login/negotiation_test.go contains TestNegotiationNumericalMin
    - internal/login/negotiation_test.go contains TestNegotiationListSelect
    - internal/login/negotiation_test.go contains TestNegotiateFullParams
    - `go test ./internal/login/ -run "TestNegotiation" -count=1` exits 0
    - `go vet ./internal/login/` exits 0
  </acceptance_criteria>
  <done>Declarative key registry covers all 14 mandatory keys, negotiation engine resolves all five types correctly, parameterized tests provide full matrix coverage per TEST-04</done>
</task>

</tasks>

<verification>
- `go test ./internal/login/ -v -count=1` -- all tests pass
- `go vet ./internal/login/` -- no issues
- `go test ./internal/login/ -race -count=1` -- no race conditions
- All 14 RFC 7143 Section 13 keys present in keyRegistry
- NegotiatedParams has typed fields (not string map) per D-05
- LoginError works with errors.As() per D-08
</verification>

<success_criteria>
1. Text codec round-trips key=value\0 pairs with deterministic ordering
2. Negotiation engine resolves BooleanAnd, BooleanOr, NumericalMin, NumericalMax, ListSelect, Declarative correctly
3. All 14 mandatory keys registered with correct types and defaults per RFC 7143 Section 13
4. NegotiatedParams has compile-time safe typed fields (not map[string]string)
5. LoginError has StatusClass/StatusDetail fields and works with errors.As()
6. Parameterized tests cover the full negotiation matrix (TEST-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-and-login/02-01-SUMMARY.md`
</output>
