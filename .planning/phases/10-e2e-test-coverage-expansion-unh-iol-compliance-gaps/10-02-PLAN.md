---
phase: 10-e2e-test-coverage-expansion-unh-iol-compliance-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/e2e/digest_test.go
  - test/e2e/scsierror_test.go
autonomous: true
requirements: [E2E-17, E2E-18, E2E-19, E2E-20]
must_haves:
  truths:
    - "Header-only digest mode negotiates and data transfers correctly"
    - "Data-only digest mode negotiates and data transfers correctly"
    - "Out-of-range LBA write returns SCSIError with ILLEGAL_REQUEST sense key"
    - "SCSIError.Error() string includes sense key name and ASC/ASCQ description"
    - "All new tests skip gracefully when not root or modules not loaded"
  artifacts:
    - path: "test/e2e/digest_test.go"
      provides: "Header-only and data-only digest E2E tests"
      contains: "TestDigest_HeaderOnly"
    - path: "test/e2e/scsierror_test.go"
      provides: "SCSI error condition E2E tests"
      contains: "TestSCSIError_OutOfRangeLBA"
  key_links:
    - from: "test/e2e/digest_test.go"
      to: "options.go"
      via: "uiscsi.WithHeaderDigest / uiscsi.WithDataDigest"
      pattern: "uiscsi\\.With(Header|Data)Digest"
    - from: "test/e2e/scsierror_test.go"
      to: "errors.go"
      via: "errors.As with uiscsi.SCSIError"
      pattern: "errors\\.As.*SCSIError"
---

<objective>
Add E2E tests for asymmetric digest modes (header-only, data-only) and SCSI error condition handling (out-of-range LBA, sense data parsing).

Purpose: Cover UNH-IOL compliance gaps for digest variant negotiation and SCSI CHECK CONDITION handling, ensuring the library correctly reports sense data from a real target.
Output: Extended digest_test.go with two new tests, new scsierror_test.go.
</objective>

<execution_context>
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rkujawa/repos/uiscsi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-e2e-test-coverage-expansion-unh-iol-compliance-gaps/10-CONTEXT.md
@.planning/phases/10-e2e-test-coverage-expansion-unh-iol-compliance-gaps/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From errors.go:
```go
type SCSIError struct {
    Status   uint8
    SenseKey uint8
    ASC      uint8
    ASCQ     uint8
    Message  string
}

func (e *SCSIError) Error() string {
    if e.Message != "" {
        return fmt.Sprintf("scsi: status 0x%02X: %s", e.Status, e.Message)
    }
    return fmt.Sprintf("scsi: status 0x%02X", e.Status)
}
```

From test/e2e/digest_test.go (existing pattern):
```go
// TestDigests uses both WithHeaderDigest("CRC32C") + WithDataDigest("CRC32C")
// New tests use each independently for asymmetric modes.
```

From test/e2e/data_test.go:
```go
const initiatorIQN = "iqn.2026-04.com.uiscsi.e2e:initiator"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header-only and data-only digest E2E tests</name>
  <files>test/e2e/digest_test.go</files>
  <read_first>
    - test/e2e/digest_test.go (existing TestDigests pattern — reuse structure, extend with two new tests)
    - test/e2e/data_test.go (initiatorIQN constant, write+read pattern)
    - options.go (WithHeaderDigest, WithDataDigest signatures)
  </read_first>
  <action>
Per D-08, D-09: Add two new test functions to `test/e2e/digest_test.go`.

1. Add `TestDigest_HeaderOnly(t *testing.T)` (E2E-17):
   - `lio.RequireRoot(t)`, `lio.RequireModules(t)` (per D-12, E2E-20)
   - Setup LIO target with `TargetSuffix: "digest-hdr"`, `InitiatorIQN: initiatorIQN`
   - `defer cleanup()`
   - 30-second context timeout
   - `uiscsi.Dial` with `WithTarget`, `WithInitiatorName`, `WithHeaderDigest("CRC32C")` — do NOT call WithDataDigest (defaults to None)
   - `sess.ReadCapacity(ctx, 0)` for block size
   - Write 4 blocks of test data (pattern: `byte(i % 199)`)
   - `sess.WriteBlocks(ctx, 0, 0, 4, blockSize, testData)`
   - `sess.ReadBlocks(ctx, 0, 0, 4, blockSize)`
   - `bytes.Equal(readBack, testData)` assertion
   - `t.Log("Header-only CRC32C digest: write+read OK")`

2. Add `TestDigest_DataOnly(t *testing.T)` (E2E-18):
   - Same structure as TestDigest_HeaderOnly but:
   - `TargetSuffix: "digest-dat"`
   - `WithDataDigest("CRC32C")` — do NOT call WithHeaderDigest (defaults to None)
   - `t.Log("Data-only CRC32C digest: write+read OK")`

Both tests follow the exact same pattern as existing `TestDigests` but with only one digest type enabled. Ensure the `bytes` import is present (already imported by existing test).
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go vet -tags e2e ./test/e2e/</automated>
  </verify>
  <acceptance_criteria>
    - test/e2e/digest_test.go contains `func TestDigest_HeaderOnly(t *testing.T)`
    - test/e2e/digest_test.go contains `func TestDigest_DataOnly(t *testing.T)`
    - TestDigest_HeaderOnly calls `uiscsi.WithHeaderDigest("CRC32C")` but NOT `WithDataDigest`
    - TestDigest_DataOnly calls `uiscsi.WithDataDigest("CRC32C")` but NOT `WithHeaderDigest`
    - Both tests call `lio.RequireRoot(t)` and `lio.RequireModules(t)`
    - Both tests use unique TargetSuffix ("digest-hdr", "digest-dat")
    - Both tests perform write+read with `bytes.Equal` verification
    - `go vet -tags e2e ./test/e2e/` exits 0
  </acceptance_criteria>
  <done>Header-only and data-only digest modes each tested with write+read cycle against real LIO target, tests skip gracefully when not root</done>
</task>

<task type="auto">
  <name>Task 2: SCSI error condition E2E tests</name>
  <files>test/e2e/scsierror_test.go</files>
  <read_first>
    - errors.go (SCSIError struct — SenseKey, ASC, ASCQ, Message fields, Error() method format)
    - test/e2e/data_test.go (initiatorIQN constant, Dial+ReadCapacity pattern)
    - test/e2e/e2e_test.go (TestMain pattern)
    - test/lio/lio.go (Setup, Config struct — default LUN is 64MB = 131072 blocks at 512B)
  </read_first>
  <action>
Per D-10, D-11: Create `test/e2e/scsierror_test.go` with SCSI error handling tests.

1. File header:
   - Build tag: `//go:build e2e`
   - Package: `e2e_test`
   - Import: `errors`, `strings`, `context`, `testing`, `time`, `github.com/rkujawa/uiscsi`, `github.com/rkujawa/uiscsi/test/lio`

2. `TestSCSIError_OutOfRangeLBA(t *testing.T)` (E2E-19, D-10):
   - `lio.RequireRoot(t)`, `lio.RequireModules(t)` (per D-12, E2E-20)
   - Setup LIO target with `TargetSuffix: "scsierr"`, `InitiatorIQN: initiatorIQN`
   - `defer cleanup()`
   - 30-second context timeout
   - `uiscsi.Dial` with `WithTarget`, `WithInitiatorName`
   - Write 1 block of zeroes to LBA 200000 (well beyond 64MB/131072-block LUN capacity):
     ```go
     oneBlock := make([]byte, 512)
     err = sess.WriteBlocks(ctx, 0, 200000, 1, 512, oneBlock)
     ```
   - Assert `err != nil` (fatal if nil: "expected error for out-of-range LBA, got nil")
   - Use `errors.As(err, &scsiErr)` where `scsiErr` is `*uiscsi.SCSIError`
   - Assert SenseKey == 0x05 (ILLEGAL_REQUEST):
     `if scsiErr.SenseKey != 0x05 { t.Errorf("SenseKey: got 0x%02X, want 0x05 (ILLEGAL_REQUEST)", scsiErr.SenseKey) }`
   - Assert ASC == 0x21, ASCQ == 0x00 ("Logical block address out of range"):
     `if scsiErr.ASC != 0x21 || scsiErr.ASCQ != 0x00 { t.Errorf("ASC/ASCQ: got 0x%02X/0x%02X, want 0x21/0x00", scsiErr.ASC, scsiErr.ASCQ) }`
   - Per D-11, verify human-readable message contains sense key name and description:
     ```go
     errMsg := scsiErr.Error()
     if !strings.Contains(errMsg, "ILLEGAL REQUEST") {
         t.Errorf("error message should contain 'ILLEGAL REQUEST': %s", errMsg)
     }
     ```
   - Log success: `t.Logf("Out-of-range LBA: got expected SCSIError: %v", scsiErr)`

3. `TestSCSIError_SenseDataParsing(t *testing.T)` (E2E-19, D-11):
   - This is a companion test that verifies sense data parsing more explicitly
   - Same setup as above (TargetSuffix: "sense")
   - Read 1 block from LBA 200000 (out of range read should also fail):
     ```go
     _, err = sess.ReadBlocks(ctx, 0, 200000, 1, 512)
     ```
   - Assert `err != nil`
   - Use `errors.As(err, &scsiErr)` with `*uiscsi.SCSIError`
   - Verify `scsiErr.Message` is non-empty (sense data was parsed, not just status byte)
   - Verify `scsiErr.SenseKey != 0` (sense data was extracted)
   - `t.Logf("Sense data parsing: SenseKey=0x%02X ASC=0x%02X ASCQ=0x%02X Message=%q", scsiErr.SenseKey, scsiErr.ASC, scsiErr.ASCQ, scsiErr.Message)`
  </action>
  <verify>
    <automated>cd /home/rkujawa/repos/uiscsi && go vet -tags e2e ./test/e2e/</automated>
  </verify>
  <acceptance_criteria>
    - test/e2e/scsierror_test.go exists with `//go:build e2e` tag
    - test/e2e/scsierror_test.go contains `func TestSCSIError_OutOfRangeLBA(t *testing.T)`
    - test/e2e/scsierror_test.go contains `func TestSCSIError_SenseDataParsing(t *testing.T)`
    - TestSCSIError_OutOfRangeLBA contains `errors.As(err, &scsiErr)` with `*uiscsi.SCSIError`
    - TestSCSIError_OutOfRangeLBA asserts `scsiErr.SenseKey != 0x05` (ILLEGAL_REQUEST)
    - TestSCSIError_OutOfRangeLBA asserts `scsiErr.ASC != 0x21` and `scsiErr.ASCQ != 0x00`
    - TestSCSIError_OutOfRangeLBA contains `strings.Contains` check for "ILLEGAL REQUEST" in error message
    - Both tests call `lio.RequireRoot(t)` and `lio.RequireModules(t)`
    - Both tests use LBA 200000 (beyond 64MB LUN capacity)
    - `go vet -tags e2e ./test/e2e/` exits 0
  </acceptance_criteria>
  <done>Out-of-range LBA returns SCSIError with ILLEGAL_REQUEST sense key, ASC 0x21/ASCQ 0x00, human-readable error includes sense key name, sense data parsing verified for both read and write errors</done>
</task>

</tasks>

<verification>
- `go vet -tags e2e ./test/e2e/` passes
- When run as root with LIO modules: `sudo go test -tags e2e -v -count=1 ./test/e2e/ -run "TestDigest_HeaderOnly|TestDigest_DataOnly|TestSCSIError"` passes
</verification>

<success_criteria>
- Header-only and data-only digest modes each pass write+read cycle
- Out-of-range LBA correctly produces SCSIError with ILLEGAL_REQUEST sense
- Error messages include human-readable sense key name and ASC/ASCQ description
- All tests use lio.RequireRoot/RequireModules for graceful skip
</success_criteria>

<output>
After completion, create `.planning/phases/10-e2e-test-coverage-expansion-unh-iol-compliance-gaps/10-02-SUMMARY.md`
</output>
