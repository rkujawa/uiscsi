---
phase: 06.1-observability-and-debugging-infrastructure
plan: 02
subsystem: observability
tags: [slog, metrics, hooks, callbacks, pdu-tracing]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "structured error types and error wrapping patterns"
provides:
  - "PDUDirection and MetricEvent types for observability consumers"
  - "WithPDUHook and WithMetricsHook session option functions"
  - "Logger injection in transport pumps (no global slog)"
  - "PDU-level debug tracing with opcode/ITT/CmdSN/StatSN/ds_len"
  - "MetricCommandComplete with latency measurement"
affects: [06.1-03, 07-e2e-testing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Push-based metric callbacks via SessionOption hook registration"
    - "Transport-layer direction constants (HookSend/HookReceive) to avoid circular imports"
    - "pduHookBridge pattern bridging transport uint8 direction to session-typed PDUDirection"
    - "slog.Enabled guard before debug log construction"

key-files:
  created:
    - "internal/session/metrics.go"
    - "internal/session/metrics_test.go"
  modified:
    - "internal/session/types.go"
    - "internal/transport/pump.go"
    - "internal/transport/pump_test.go"
    - "internal/session/session.go"
    - "internal/session/datain.go"

key-decisions:
  - "Direction constants (HookSend/HookReceive) in transport package to avoid session->transport circular dependency"
  - "pduHookBridge returns nil when no hooks configured, enabling zero-cost path in hot loop"
  - "MetricEvent uses push-based callback (no concrete stats struct) per D-11 design decision"

patterns-established:
  - "Hook bridge pattern: transport uses plain uint8 direction, session bridges to typed PDUDirection"
  - "Metrics emission outside mutex: hooks called in pump goroutines, not under session lock"

requirements-completed: [OBS-01, OBS-03]

# Metrics
duration: 4min
completed: 2026-04-01
---

# Phase 06.1 Plan 02: PDU Hook Callbacks and Metrics Event System Summary

**PDU hook callbacks and metrics events with injected logger, eliminating global slog in transport pumps**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-01T21:00:07Z
- **Completed:** 2026-04-01T21:04:32Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Defined PDUDirection, MetricEventType, and MetricEvent types with WithPDUHook/WithMetricsHook session options
- Injected slog.Logger into ReadPump/WritePump, replacing all global slog calls in transport
- Added PDU-level debug tracing with opcode, ITT, CmdSN/StatSN, and data segment length
- Wired metrics emission for PDU send/receive, bytes in/out, and command completion with latency

## Task Commits

Each task was committed atomically:

1. **Task 1: MetricEvent, PDUDirection types, and session option functions** - `43dfc6d` (feat)
2. **Task 2: Pump logger injection, PDU hooks in pumps, metrics emission in session** - `c520d93` (feat)

## Files Created/Modified
- `internal/session/metrics.go` - PDUDirection, MetricEventType, MetricEvent types, WithPDUHook/WithMetricsHook options
- `internal/session/metrics_test.go` - Unit tests for all types and option functions
- `internal/session/types.go` - Added pduHook and metricsHook fields to sessionConfig
- `internal/transport/pump.go` - Updated ReadPump/WritePump with logger, pduHook params; HookSend/HookReceive constants; debug tracing
- `internal/transport/pump_test.go` - Updated all existing tests with new params; added hook and logger tests
- `internal/session/session.go` - pduHookBridge method; updated pump call sites; MetricCommandComplete emission
- `internal/session/datain.go` - Added startTime field to task struct for latency measurement

## Decisions Made
- Used uint8 direction constants in transport package to avoid circular dependency between session and transport
- pduHookBridge returns nil when no hooks configured for zero overhead in the common no-hooks path
- MetricEvent is push-based callback per D-11: no prescribed stats struct, consumers aggregate freely

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- PDU hooks and metrics callbacks ready for consumers in plan 03 (PDU dumper, test helpers)
- Logger injection complete; all transport logging now uses injected slog.Logger

## Self-Check: PASSED

All 7 files verified present. Both task commits (43dfc6d, c520d93) verified in git log.

---
*Phase: 06.1-observability-and-debugging-infrastructure*
*Completed: 2026-04-01*
